/**
 * @typedef {number} NSTextAlignment
 */

/**
 * @typedef {number} UIControlEvent
 */

/**
 * @typedef {number} UIControlState
 */

/**
 * @typedef {{ borderWidth: number, borderColor: Color, cornerRadius: number, shadowColor: Color, shadowOpacity: number, shadowOffset: Size }} Layer
 */

/**
 * @typedef {{ red: number, green: number, blue: number, alpha: ?number }} Color
 */

/**
 * @typedef {{ x: number, y: number, width: number, height: number }} Frame
 */

/**
 * @typedef {{ width: number, height: number }} Size
 */

/**
 * @typedef {[Function, UIControlEvent]} Target
 */
