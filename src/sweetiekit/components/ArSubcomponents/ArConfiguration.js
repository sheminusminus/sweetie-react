const SweetieKit = Require('std:sweetiekit.node');

const {
  ARWorldTrackingConfiguration: ArWorldTrackingConfiguration,
  // ARBodyTrackingConfiguration: ArBodyTrackingConfiguration,
  // AROrientationTrackingConfiguration: ArOrientationTrackingConfiguration,
  // ARImageTrackingConfiguration: ArImageTrackingConfiguration,
  // ARFaceTrackingConfiguration: ArFaceTrackingConfiguration,
  // ARObjectScanningConfiguration: ArObjectScanningConfiguration,
} = SweetieKit;

export {
  ArWorldTrackingConfiguration,
  // ArBodyTrackingConfiguration,
  // ArOrientationTrackingConfiguration,
  // ArImageTrackingConfiguration,
  // ArFaceTrackingConfiguration,
  // ArObjectScanningConfiguration,
};
